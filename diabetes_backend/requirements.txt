# Diabetes Management Platform - Backend Dependencies
# ===================================================
# This file contains only the essential packages required for the application
#
# ⚠️  IMPORTANT: This is a CLEAN, minimal requirements.txt file
# ⚠️  The previous file had 700+ unnecessary packages from development environment
# ⚠️  This version contains ONLY what the application actually needs

# Core Flask Framework
Flask==3.1.0
Flask-Bcrypt==1.0.1
Flask-CORS==5.0.1
Flask-JWT-Extended==4.7.1
Flask-SocketIO==5.5.1

# Database
pymongo==4.12.1

# Environment & Configuration
python-dotenv==1.1.0

# File Processing & OCR
pytesseract==0.3.13
PyMuPDF==1.25.5
Pillow==11.2.1

# HTTP Requests
requests==2.32.3

# Security & Password Hashing
Werkzeug==3.1.3
bcrypt==4.3.0

# Date/Time Utilities
python-dateutil==2.9.0.post0

# Additional dependencies that may be needed
python-socketio==5.13.0
python-engineio==4.12.0
blinker==1.9.0
itsdangerous==2.2.0
click==8.1.8
MarkupSafe==3.0.2
Jinja2==3.1.6

# BSON/ObjectId Support (included with pymongo)
# bson is included with pymongo

# Standard Library Extensions
# os, sys, datetime, logging, traceback are built-in Python modules
